# Maximum accepted PDF size in bytes (default: 10 MB)
MAX_UPLOAD_SIZE_BYTES=10485760

# Comma-separated browser origins allowed to call the API
FRONTEND_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
